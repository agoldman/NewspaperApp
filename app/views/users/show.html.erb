<h1>User Profile for <%= @user.name %></h1>

<h2>Subscriptions</h2>
<ul>
<% @user.subscription_plans.each do |sp| %>
	<li> <%= link_to sp.newspaper.title, newspaper_url(sp.newspaper) %>: <%= sp.weekly ? "Weekly" : "Daily" %> for <%= sp.price %></li>
<% end %>
</ul>